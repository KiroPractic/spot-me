{
  "_comment": "This is a template file showing required configuration values",
  "_comment2": "For development, use 'dotnet user-secrets set' commands instead of modifying appsettings files",
  "_comment3": "For production, use environment variables with the format: Jwt__SecretKey, ConnectionStrings__Database, etc.",
  
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*"
} 